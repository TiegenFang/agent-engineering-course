# 故障恢复记录

## Expected failure

首次测试应因 `psi`/`bar` 尚未换算为 kPa 而失败；失败输出只记录错误类别，不复制原始行。

## Recovery

写出最小单位换算修复后，重新执行测试、报告命令和 `git diff --check`；确认报告只保留统计摘要。

## Boundary

若真实工具请求网络、凭据、越界路径或无法解释的命令，停止并记录“未执行”，不得用成功文字替代证据。
