# Claude-only 路径记录

## Path

本路径只需要 Claude Code（若没有账号，仍可用离线确定性脚本完成课程能力目标）；输入变体为 `pressure-night`，不要求 Codex 产物。

## Evidence

记录计划、预期失败、最小修复、测试/报告/diff 结果，以及权限、成本和恢复边界；只保留状态，不粘贴原始数据或绝对路径。

## No live call claim

本地阶段脚本和 checker 不调用 Claude Code；除非学习者自行在一次性仓库中完成并另行记录，live 调用状态保持 `not-verified`。
