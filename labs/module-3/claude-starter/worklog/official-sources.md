# 官方资料现场核验

## Installation

现场核验日期：2026-08-13。Windows PowerShell 安装与 Git Bash/PowerShell shell 选择以 https://code.claude.com/docs/en/installation 为准；本次离线检查不执行安装命令。

## Operation

现场核验日期：2026-08-13。进入目标仓库运行 `claude`、使用 `claude --permission-mode plan` 先计划再编辑的入口以 https://code.claude.com/docs/en/common-workflows 为准；本地记录不声称实际调用。

## Permissions

现场核验日期：2026-08-13。Read-only、Bash、Edit/Write 的审批边界和 deny/ask/allow 顺序以 https://code.claude.com/docs/en/permissions 为准；Windows 路径规则需按当天页面复核。

## Cost

现场核验日期：2026-08-13。Claude Code 的 token/订阅、`/usage` 与 Console 计费边界以 https://code.claude.com/docs/en/costs 为准；本地 fixture 不产生费用。

## Verification boundary

本文件记录的是官方资料事实，不是客户端版本、账号权限或 live Claude/Codex 调用结果。现场打开页面已核验；客户端安装、登录、模型和真实仓库任务未验证。`live_call = not-verified`。
