# Codex 对照记录

## Codex reference

Codex 主实验已在课程基线 `0a10780` 合入，课节 ID 为 `t04-codex-repository-task`。Claude-only 路径不要求 Codex；双工具路径只记录该主实验的状态摘要和边界，不复制其温度/Fahrenheit 输入，也不把状态摘要当作 live 调用证明。

## Changed input

本任务改为压力、`kPa`/`psi`/`bar`、峰值和告警计数；Codex 主实验的温度、`C`/`F` 与均值字段不能直接替代本合同。

## No live call claim

本记录不包含账号、token、路径、源码或模型输出；未进行的 Codex/Claude Code 调用保持 `not-verified`。
